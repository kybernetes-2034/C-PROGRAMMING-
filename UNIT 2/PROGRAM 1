//1.Demonstrate order of execution of operators

#include <stdio.h>

int main()
{
    int a, b, c, result;

    printf("Enter three numbers: ");
    scanf("%d %d %d", &a, &b, &c);

    result = a + b * c;

    printf("Result = %d", result);

    return 0;
}
